# 이 폴더는 저장소에 들어가지 않는다

로컬 전용 문서 자리다. `.gitignore`의 `docs/contract/*` 로 제외되며, 이 README만
예외로 추적한다 — 폴더가 통째로 무시되면 다음 사람이 "왜 비어 있지"를 알 수 없다.

디렉터리째 제외(`docs/contract/`)하면 git이 안으로 들어가지 않아 `!` 예외가
무력화된다. 그래서 `docs/contract/*` 로 내용만 제외한다.
