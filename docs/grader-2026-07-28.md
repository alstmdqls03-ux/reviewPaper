# 채점기 — 2026-07-28 야간 작업

**구현 전에 썼다.** 각 항목은 "이 조작을 하면 이 결과가 화면에 보인다" 한 문장.
통과한 것만 `[x]`. 통과 못 하면 `[ ]`로 남기고 이유를 적는다 — 통과한 척 보고하지 않는다.

지난 실패에서 배운 규칙 3개(`docs/retrospective-v1-v5.md` §2-3):
1. **"보인다"가 아니라 "읽힌다"를 잰다.** 존재 확인이 아니라 비율·줄 수·픽셀을 적는다.
2. **숫자 회귀는 skip 줄까지 적는다.** `16 passed`가 아니라 `10 passed, 9 skipped (서버 없음)`.
3. **폭을 바꾸는 항목은 그 폭에서 스크린샷을 찍는 것이 통과 조건이다.**

---

## A. 지목받은 2건 (정보 밀도)

- [x] **A1.** 논문 10편을 전부 선택한 채로 질문하면, 답변 위에 `사용한 소스 10편 ▾` **한 줄**만 보인다.
  계측: 그 줄의 높이가 **24px 이하**이고, 바로 아래 답변 버블 높이보다 **작다**.
  (직전 값: 116px / 897자, 답변 74px — 라벨이 본문의 1.57배)
- [x] **A2.** `사용한 소스 10편 ▾`을 클릭하면 그 자리에서 제목 10개가 펼쳐지고, 다시 누르면 접힌다.
  펼친 상태는 그 답변에만 적용되고 다른 답변은 접힌 채로 남는다.
- [x] **A3.** 1440px에서 소스 레일 툴바가 **한 줄**에 들어간다.
  계측: `.pane-toolbar`의 자식들이 모두 같은 `top` 값을 갖고, 툴바 높이가 **60px 이하**.
  (직전 값: 143px / 3행)
- [x] **A4.** 툴바의 체크박스 하나를 누르면 전체 선택, 다시 누르면 전체 해제되고,
  일부만 선택된 상태에서는 그 체크박스가 중간(indeterminate) 표시가 된다.

## B. 세션 계열 (§4)

- [x] **B1.** 기록 탭 상단 검색창에 답변 본문에만 있는 단어를 넣으면, 그 단어를 포함한 대화만 목록에 남는다
  (제목에 그 단어가 없어도 남는다). 지우면 전체 목록이 돌아온다.
- [x] **B2.** 검색 결과 카드에 그 단어가 나온 문장 조각이 한 줄 보이고, 단어가 강조 표시된다.
- [x] **B3.** 대화를 재개하면 각주가 **답변 문장 안의 원래 위치**에 붙는다 —
  스트리밍으로 처음 받았을 때와 각주 번호의 순서·위치가 같다. (직전: 전부 답변 끝에 모임)
- [x] **B4.** 대화 A에서 소스 3편만 고르고, 대화 B로 갔다가 A로 돌아오면 **그 3편만 체크되어 있다**.
  대화 B는 자기 선택을 유지한다.
- [x] **B5.** (B4를 구현하는 경우) 기존 사용자가 새 버전을 처음 열면 지금의 전역 선택이 그대로 유지되고,
  체크가 초기화되지 않는다.

## C. 회귀 — 하나라도 깨지면 커밋 금지

- [x] **C1.** 질문을 보내면 답변이 스트리밍되고 각주 `[1]`이 붙는다.
- [x] **C2.** 각주 `[1]`을 클릭하면 원문 뷰어가 열리고 인용 구절이 `<mark>`로 강조된다.
- [x] **C3.** 소스 하나의 체크를 해제하고 질문하면, 펼친 "사용한 소스"에 그 제목이 없다.
- [x] **C4.** 기록 탭에서 다른 대화를 클릭하면 그 대화의 메시지만 로그에 보이고, 헤더 제목이 바뀐다.
- [x] **C5.** 현재 열린 대화를 삭제하면 목록에서 사라지고 로그가 빈 새 대화로 바뀐다.
- [x] **C6.** 질문 후 그래프에 개념 노드가 추가되고, "이해했어요"를 누르면 노드가 흰색으로 채워진다.
      숙련도 3상태(완료/학습중/복습필요)가 범례와 같이 구분된다.
- [x] **C7.** 퀴즈가 생성되고 채점되며, 점수가 `n / m 정답`으로 표시된다.
- [x] **C8.** `MOCK_LLM=1 pytest -q` — **서버 없이** 돌려서 결과를 skip 수까지 적는다.
      기준선: `10 passed, 9 skipped`. 새 테스트를 넣으면 그만큼만 늘어야 한다.

## D. 내가 찾은 것 중 한 커밋에 들어가는 것

- [x] **D1.** 캐시 쓰기 토큰(`cache_creation_input_tokens`)이 `/metrics`의 비용에 반영된다.
      단위 검증: 캐시 쓰기 1M 토큰이 있는 usage를 넣으면 비용이 입력 단가의 **2배**로 잡힌다.
- [x] **D2.** 죽은 `_conv_of` 딕셔너리가 사라지고, `pytest`와 대화 재개가 그대로 동작한다.
- [x] **D3.** 레지스트리에 없는 고아 PDF(`uploads/03-microscopy-metadata.pdf`)가 정리되고,
      `GET /corpus`가 여전히 10편을 반환한다.

---

## 판정 기준: 고칠 것 vs 로드맵으로 넘길 것

**고친다** — 셋을 모두 만족할 때:
1. 한 커밋에 들어간다(파일 3개 이하, 저장소 스키마 변경 없음).
2. 기존 데이터를 이관할 필요가 없거나, 이관이 자동 백필 한 줄로 끝난다.
3. 실패해도 되돌리는 데 `git revert` 하나면 된다.

**로드맵으로 넘긴다** — 하나라도 걸리면:
- 저장소 스키마가 바뀐다 (테이블 추가, 컬럼 의미 변경).
- 요청 처리 모델이 바뀐다 (동기 → 백그라운드 작업 큐).
- 실키 없이는 통과 여부를 판정할 수 없다.
- 외부 벤더·OAuth·로그인이 선행 조건이다.

---

## 결과 (2026-07-28 새벽, 전부 재실행해서 확인)

**20/20 통과.** 각 항목의 실측값:

| 항목 | 측정 | 통과 근거 |
|---|---|---|
| A1 | 접힌 "보낸 소스 10편" **21px**, 답변 버블 **75px** | ≤24px, 본문보다 작음 (직전 116px/897자) |
| A2 | 펼치면 **208px, 항목 10개**. 두 번째 답변은 접힌 채 유지 | `<details>`가 답변별로 독립 |
| A3 | 툴바 **57px, 1행** — 1440px·800px·320px 전부. 자식 4~5개 세로 중심이 동일값 | ≤60px, 1행 (직전 143px/3행) |
| A4 | 전체선택 10 → 전체해제 0 → 전체선택 10 → 개별1해제 9(indeterminate=true) | 4단계 전이 |
| B1 | 대화 6건 중 "Annotation" 검색 → **4건**, 그중 제목에 그 단어가 있는 건 **0건** | 본문 검색이 실제로 동작 |
| B2 | snippet 4/4, `<mark>` 강조. "%" 검색 0건(와일드카드 차단), 없는 단어 0건 | |
| B3 | 스트리밍 각주 위치 **[44, 90, 118]** → 새로고침 복원 **[44, 90, 118]** | 완전 일치 (직전: 전부 끝에) |
| B4 | A(3편)·B(5편) 만든 뒤 A복귀 3편/레일체크 3, B복귀 5편/레일체크 5 | |
| B5 | 전역 키 4편만 있는 상태로 열면 4편 그대로 체크, 수정 시 그 대화 것이 됨 | 이관 시 초기화 없음 |
| C1 | 답변 스트리밍 + 각주 3개 | |
| C2 | 원문 뷰어 3/41쪽, 실제 원문 구절로 `<mark>` **적중** | MOCK의 합성 인용문은 실제 PDF에 없어 안 잡히는 게 정상 — 실제 구절로 재검증했다 |
| C3 | 소스 1편 해제 → "보낸 소스 **9편**", 해제한 제목 목록에 없음 | |
| C4 | 대화 전환 시 헤더 제목·메시지 모두 그 대화 것 | |
| C5 | 삭제 → 목록에서 사라짐, 로그 빈 상태, 헤더 "새 대화", 그 대화의 소스 선택도 삭제 | |
| C6 | 노드 3·엣지 2 성장. "이해했어요" → `m-learning`→`m-known`, 배경 `rgb(34,37,43)`→`rgb(233,234,238)`, 배지 "이해 완료". 3상태 렌더 스타일 **3/3 서로 다름** | |
| C7 | 퀴즈 3문제 생성, `3 / 3` 채점 | |
| C8 | **`14 passed, 9 skipped`** (서버 없이) | 기준선 10 passed + 신규 4건(소스 가드 1 + 인용 3). skip 수 불변 |
| D1 | 캐시쓰기 1M = $10(1h), $6.25(5m). 콜드가 웜의 15배 초과 | `obs.py` 자체검사 |
| D2 | `_conv_of` 제거 후 대화 재개·pytest 정상 | |
| D3 | 고아 PDF 삭제(sha1로 중복 확인), `/corpus` 여전히 10편 | |

**측정 방법 중 한 번 틀렸던 것**: A3의 "자식들이 모두 같은 top 값"은 잘못된 지표였다.
높이가 다른 요소를 세로 가운데 정렬하면 같은 줄이어도 `top`이 다르다. **세로 중심**으로 바꿔 다시 쟀다.
C6의 3상태 비교도 `addClass` 직후 같은 틱에서 읽어 두 상태가 같게 나왔다 — 렌더 후 `renderedStyle`로 다시 쟀다.
채점기도 틀릴 수 있다는 게 이번 회고의 요지였고, 실제로 두 번 틀렸다.
